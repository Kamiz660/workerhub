/**
 * Centralized event name constants.
 * Use these instead of raw strings for CustomEvent dispatch/listen.
 */
export const EVENTS = {
  OPEN_ADD_WORKER_MODAL: "open-add-worker-modal",
  WORKER_ADDED: "worker-added",
} as const;
